// Program to calculate square of a number
#include<stdio.h>
int main()
{
    int num;
    printf("Enter a number:");
    scanf("%d",&num);
    printf("Square of %d is:%d",num,num*num);
    return 0;
}

// This similar to C code 
// All C code can be compiled and run by g++ also
// All C syntax header files are valid in C++

/* In this code eg. we learn about exit code,return value and it's compatibility with the return type of main() function,function definition in C++ */

int main()
{
    //return 0; 
    /* - return value is 0 which is compatible with return type of main() function
       - 0 indicates a successful execution without errors and also successful termination*/
    //return -1; 
    /* return value is -1 which indicates failed execution of program  */
}